# Catalog is LiteLLM :4000. Self-hosted default is Beelink Ollama.
# Per-agent overrides live in agents.yaml.
model:
  provider: openai
  credential_secret_ref: openai-api-key
  default_model: gemma4-12b-beelink
