# Legacy API metadata required by the tenant schema. Beelink executes all Bots through
# OWNBOT_ACP_CONFIG with cliOnly=true; this value never selects an inference backend there.
model:
  provider: openai
  credential_secret_ref: openai-api-key
  default_model: gemini-3.8-flash
