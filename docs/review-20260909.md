# Beelink review — September 9, 2026

Codex coordinated this review with the existing interactive cmux sessions: Claude Code Opus 5 reviewed and implemented backend fixes; Pi reviewed the channel UI. Work was isolated in a Git worktree based on 3501839, with a separate migrated PostgreSQL database. Existing tenant changes removing Telegram notification grants and updating the Pi skill were retained.

## Corrections

- Late channel events no longer replace newer previews, duplicate events preserve cache identity, and malformed socket frames are ignored. Pi supplied the review and regression cases; Codex implemented and tested the patch. Pin, busy and deletion events keep their separate behavior.
- Distinct connectors with duplicate operation names now receive distinct, stable ACP names. The production grant table confirmed Pi M4/M5 grants on CodeExec and other coworkers. Executors and grant references remain unchanged; the ambiguous short name cannot select a connector arbitrarily.
- Invalid routine timeout and research-call settings fail visibly at startup. The timeout bound accounts for the abort grace period, avoiding timer overflow into an immediate cancellation.
- ACP failure logs include a bounded cause classification without copying CLI errors, prompt content or credential diagnostics.
- A failure to inspect publication metadata no longer discards already extracted page text. Missing metadata remains unverified.
- FT landing pages cannot satisfy article evidence, whitespace cannot inflate the minimum excerpt, and required sources must be cited in the report itself to satisfy coverage.
- Pi ACP tests launch their fixture with the current Bun executable rather than a macOS Homebrew path. This leaves the production sandbox environment unchanged.

## Verification

After backend and UI integration, the complete suite passed 2,515 tests with 39 explicit skips and no failures (234 files). The database was `openbot_review_20260909`, never the production database. Initial test failures exposed missing worktree dependency links and the macOS-only Pi fixture; these were corrected and the complete suite rerun. Root build and all workspace typechecks passed. Vite retains its existing large-chunk advisory. Repository-wide lint and formatting remain failing on pre-existing debt: the unchanged original reported 11 lint errors, 120 warnings and 99 formatting errors. New modules were checked separately; this review does not claim globally clean lint.

The computer TypeScript check also passed using the exact dependencies copied from the candidate image. The candidate computer image was built from Dockerfile.beelink. An isolated, network-disabled Chromium smoke check verified real metadata extraction and a closed-page fallback without accessing saved profiles. This does not certify current paid-news coverage or vendor inference availability. No external notifications, live routine redispatch or trading actions were used as tests.

The normal web login page loaded in the cmux browser. Authenticated end-to-end chat and end-device push receipt were not exercised in that browser session.

## Recovery

Before deployment, `openbot-backup-review-20260909` retained a verified PostgreSQL archive, a SQLite backup with integrity check, the previous UI build, and the pre-existing tenant diff. Keep the prior computer image tagged for rollback. Database schema, model selections, grants beyond the retained tenant edits, and saved browser profiles are not changed by these fixes.


## Deployment result

The tenant edits were committed separately as `8a7c707`; reviewed fixes were applied as `3d4999e` on `beelink-local`. The UI was replaced during a short service restart window, retaining old hashed assets for already-open clients. The computer image was rebuilt from the final source and only the agent-computer Compose service was recreated; profiles and workspace volumes were preserved. The old image remains tagged `openbot-agent-computer:before-review-20260909`.

After restart, server/UI/worker were active, `/ready` reported all six checks true (database, scheduler, notifications, history, handoffs, runs), the static server check passed, and agent-computer was running/healthy. No unfinished routine or leased queue item was present immediately before deployment. Authenticated chat inference remains outside the live acceptance performed in this review.

## Authenticated E2E follow-up — September 9–10, 2026

The follow-up used the existing authenticated Codex in-app browser, through computer use. Source started at `0be88d0`; the isolated worktree is `/home/viniciuspinho/openbot-e2e-20260909`. The previous review worktree had retained changes and was left intact. All automated tests explicitly set both `DATABASE_URL` and `TEST_DATABASE_URL` to `openbot_review_20260909`. No test suite ran against production. Browser actions used the normal signed-in application.

### Reproductions and fixes (`fc0eae8`)

1. **Provider diagnostics polluted replies and previews.** The first authenticated prompt returned the requested `E2E-CHAT-OK`, but a Codex WebSocket 503/fallback warning was prepended and persisted. The installed `@agentclientprotocol/codex-acp` 1.10.0 source confirmed that unnegotiated notices become `agent_message_chunk`. Ownbot now negotiates its typed `sessionFailure` extension for Codex only. Notices produce bounded diagnostic logs without vendor title/details; they never count as assistant output or a successful final answer. Provider/model settings and the permission gate were not changed.
2. **Cancellation lost the user message from persisted history.** Stop released the composer and the next prompt succeeded, but navigating away/back showed the partial assistant response before the cancelled question. The local runner inserted inbound messages only on success. Success and failure now share persistence so cancellation and pre-output errors retain the question, exactly once, before partial output.
3. **Internal handoff instructions appeared as user chat bubbles.** Pi completion callbacks displayed raw `agent_result`/`pi_receipt` instructions in the source channel. Handoff delivery already supplied a separate `persistedInputMessages` transcript, but the local runner ignored it. It now honors that transcript, including an explicit empty list, and removes internal input from live/persisted `RUN_STARTED` metadata. The model still receives the complete authorized context. A regression test caught duplicate display text during implementation; the inbound transcript now supplies that text only once.

### Browser evidence before deployment

Test channel: `channel_aaeaab79-f383-4f75-a17d-724f4e0075d8`.

| Step | Observation |
| --- | --- |
| Start Coord chat with unique marker | Authenticated submission, busy/Stop state, final `E2E-CHAT-OK` |
| Reload and ask for previous code | History loaded; answered `LARANJA-731` |
| Stop a long response and send another prompt | Stop settled; next response `CANCELAMENTO-OK`; ordering fault reproduced on return |
| Navigate to routines while chat works, then return | Response completed in background and appeared in the original channel |
| Delegate once to Code, then one background job per Pi worker | Accepted handoff `hop:a405e5270d42b8d0bc4a4c2b4c3f2188:c07d90b8c2583ffa9883f8a1f1e4c5cb`; initial worker state correctly reported as queued |
| Await automatic terminal returns | Both callbacks reached the same source channel, with actual completed receipts and result 323 |
| Notifications: mark newest item read, reload, open execution | Unread count changed 23→22 and persisted; matching completed execution opened; read state restored afterward |
| Routines and operations view | Existing schedules and paused entries loaded; all six health checks passed; historical editorial failure remained visibly failed and was not rerun |

Pi receipt evidence (UTC; no worker files changed, no worker tool calls):

| Worker | Job ID | Accepted | Completed | Result |
| --- | --- | --- | --- | --- |
| M4 | `5c4bf60468d478b7f90902f660da832a` | 02:34:49.026 | 02:35:07.955 | `REVIEW-20260909-PI, Pi M4, 323` |
| M5 | `0259baec98c694ab45aaeee5afe2e9cf` | 02:34:49.251 | 02:35:10.763 | `REVIEW-20260909-PI, Pi M5, 323` |

### Automated validation and deployment

- Targeted ACP/local-history regression suite: **25 passed**.
- Complete suite: **2,521 passed, 39 explicit skips, zero failures**, 234 files. Initial attempts exposed missing agent-bot/agent-langgraph/Pi dependency links in the new worktree; these were repaired and the complete suite rerun. No dependency or lockfile changes were shipped.
- Additional routine sweep/event and notification durability checks: **29 passed** on the isolated database, with `NOTIFICATIONS_TEST_DATABASE_URL` explicitly set too. These cover concurrent/repeated dispatch, restart recovery, owner isolation, transactional notifications and persisted read state.
- Root build and app/server/worker typechecks passed. The existing large-chunk build advisory remains. Repository-wide lint/format debt reported above was not part of this patch.
- The main Beelink checkout fast-forwarded to `fc0eae8`; only `openbot-server.service` restarted at 23:43:15 BRT. Server/UI/worker remained active and `/ready` returned all six checks true. Existing UI build, computer image, profiles, model choices, grants and tenant edits were preserved.

Device push delivery remains unverified: this browser reports notifications blocked. No Telegram reactivation, external messages, trading, existing routine redispatch or production database test suite was used.

### Live retest and UI corrections (`ebc3e84`)

After the server deployment, channel `channel_87f0a29b-4fe6-4698-9f3a-5ddeb499f402` returned `CHAT-LIMPO-OK` without provider diagnostics. A real streamed list was stopped after 226 lines; immediate reload retained its question and complete partial answer in order. One new, separately keyed Pi M4 job (`d6c8a036b87447b67d0d944189ea76c6`) then progressed from queued to completed and automatically returned `RETURN-FIX-M4 667`, without raw orchestration bubbles. Earlier jobs were not resubmitted.

Navigating between channels during that callback reproduced a fourth fault: older messages moved below the new question, and the browser retained only 154 of the 226 cancelled lines. A read-only inspection of this test thread's SQLite events confirmed that all 226 lines and canonical order remained durable. The UI had merged only unseen IDs, so partial reconnect snapshots permanently won over complete same-ID messages. Both mount restoration and background callback refresh now share an ordered history merge that completes prefix text/tool arguments, preserves newer live content and local-only tool rows, and deduplicates IDs. Delayed restoration reads fresh history; unmounted refreshes cannot mutate another channel.

A fifth fault appeared in the narrow Codex browser: selecting a sidebar link changed the URL but left the modal drawer covering the destination. The app sidebar now closes that drawer when navigation resolves. Browser retest opened the corrected chat directly from Routines, with the drawer dismissed, original questions first, all 226 cancelled lines, and subsequent Pi replies in order.

Validation after the history fix: **2,526 passed, 39 explicit skips, zero failures** (235 files). After the final drawer adjustment, all **273 app tests** passed; app typecheck and production build passed. The merge module and its regression tests passed targeted lint/format checks. A test-only union-type assertion initially failed typecheck and was corrected before deployment. No runtime test failed in the final passes.

The UI was built with `TENANT_PACKAGE_DIR=/home/viniciuspinho/openbot/examples/beelink`; the generated brand remained `beelink` / `ownbot`. The main checkout fast-forwarded to `ebc3e84`. Only the UI service restarted for this deployment, retaining prior hashed assets for open clients. The previous UI is preserved in `/home/viniciuspinho/openbot-backup-e2e-ui-20260910/dist`. Static/API checks passed and all six readiness checks remained true.

### Controlled routine and notification result

The browser requested an idempotent list/create/pause/list sequence for `REVIEW-20260909-ROUTINE`. Exactly one test routine was created: `routine_bde3997d-2cd2-4e6c-a739-6b88a3624da5`, cron `59 23 31 12 *`, timezone `America/Sao_Paulo`. The Routines page independently showed its instruction, **Never run yet**, **Paused**, and switch value 0. That initial phase stopped at registration and pause; existing schedules were unchanged. Dispatch, duplicate suppression and durable notification production were also exercised by the isolated integration tests above. The subsequent single live scheduler check is recorded below.

The notification read-state restoration was independently confirmed in the sidebar at **23 unread**. Existing execution detail links and internal delivery status worked. End-device push remains unverified because browser permission is blocked; it was not enabled. The hypothesized ACP permission-before-tool-update failure did not appear in the controlled E2E and its gate was left unchanged.

### Final Pi return with the corrected UI

A separately keyed Pi M5 check (`REVIEW-20260909-UI-FINAL-M5`) ran after the UI deployment. The user turn recalled **AZUL-914** correctly, then reported handoff `hop:5c9bdc51398e17fab7baabbb1b610f10:6c4098776a20e9a03e16c0e0d2b3a8bf` as accepted/queued. The worker job was `3471e65f0645a15edc4dfaa75f02cd4b`. Codex navigated to the other review channel while processing and returned before terminal delivery. The original question, all 226 cancelled lines, earlier handoff replies and new question stayed in order. The queued callback appeared automatically, followed by the completed result **UI-FINAL-M5 1147** without reload, internal prompt bubbles or provider diagnostics. The worker receipt reported no tool calls or file changes. No previous job was resubmitted.

### Single live scheduled firing and final history cleanup (`2f6fef2`)

The same test routine was updated through the authenticated chat, after listing it, to `4 0 10 9 *` in `America/Sao_Paulo`. The confirmed next occurrence was `2026-09-10T03:04:00.000Z`. The normal scheduler started exactly one observed execution at **00:04:11 BRT**, `routine_run_adb6c8cb-5588-43c9-a9fa-744bc8d0b11d`. Its execution page showed **Concluída · Notificação interna disponível** and **ROTINA-OK**; its channel link opened the same result in the original channel. There was no manual execution or duplicate routine creation.

The test routine was paused using its own UI switch immediately afterward. The UI confirmed **Paused**, switch value 0. The notification inbox independently showed **Rotina concluída · coord**, **00:04:23 BRT**, **ROTINA-OK**, and a link to that exact execution. Unread count advanced 23→24; marking only the new test notification read and reloading returned it to **23** with the read state retained. Existing notifications were left in their original state.

This live firing exposed the same presentation defect in the routine path: the English scheduling frame appeared in a user bubble. Unlike handoff delivery, this path explicitly persisted its framed model input. It now persists only the original instruction with the same message ID, while the model still receives the firing frame and full research-budget guidance. Existing editorial gates, timeouts and history deduplication stay intact; old saved records were not rewritten. The change passed **53 routine/local-history tests** and the server typecheck. The controlled live occurrence was not repeated merely to test this text projection; the regression tests check model/visible separation and retained research guidance, alongside the already verified local runner persistence contract.

### Final acceptance

The complete suite was rerun after the routine projection change: **2,526 passed, 39 explicit skips, zero failures**, **6,507 assertions**, 235 files. The Beelink main checkout received `2f6fef2` and the server restarted successfully. Server/UI/worker were active; `/ready` again returned all six checks true, and the static UI/API check passed. The final authenticated prompt returned **CHAT-FINAL-OK AZUL-914**; reload retained the same reply and ordered history.

Implementation commits: `fc0eae8` (ACP notices and local persistence), `ebc3e84` (ordered/complete UI history and drawer navigation), `2f6fef2` (routine visible input). Publication uses the authenticated Mac, without copying credentials to Beelink. The test routine remains paused; its single notification was marked read. Models, profiles, existing grants and earlier tenant improvements were preserved. End-device push delivery is still outside the verified result; the browser explicitly blocks it. Existing historical diagnostic bubbles are retained as prior records, not retrospectively edited.
